import { msg } from '@lingui/core/macro';

export const FILE_MAX_DESCRIPTION_LENGTH = 240;
export const FILE_LOCATION_DESCRIPTION = msg`Note: all location data can be made private on the main create post form.`;
