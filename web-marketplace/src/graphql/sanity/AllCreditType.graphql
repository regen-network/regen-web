query allCreditType {
  allCreditType {
    language
    name
    largeImage {
      ...imageFields
    }
    unit
    unitDefinitionRaw
    category {
      name
      icon {
        ...imageFields
      }
      largeImage {
        ...imageFields
      }
    }
  }
}
