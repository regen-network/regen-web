import { makeStyles } from 'tss-react/mui';

const useMyEcocreditsStyles = makeStyles()(theme => ({
  arrow: {
    width: theme.spacing(6),
    height: theme.spacing(6),
  },
}));

export default useMyEcocreditsStyles;
