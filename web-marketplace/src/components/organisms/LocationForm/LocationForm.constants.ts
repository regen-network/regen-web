import { msg } from '@lingui/core/macro';

export const LOCATION_LABEL = msg`Project location`;
export const LOCATION_DESCRIPTION = msg`Type an address or latitude, longitude coordinates in decimal degree format (i.e. 41.40338, 2.17403). Note: if you enter rural lat/long coordinates, select the closest town (the map marker on your project page will still appear in the correct location).`;
export const LOCATION_PLACEHOLDER = msg`Start typing the location`;
