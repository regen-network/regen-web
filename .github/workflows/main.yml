name: CI

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: install
      run: |
        yarn install
        yarn bootstrap

    - name: build
      run: yarn build

    - name: test
      run: yarn test
