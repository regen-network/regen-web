query partyByAddr($addr: String!) {
  walletByAddr(addr: $addr) {
    id
    partyByWalletId {
      id
      name
      type
      image
      bgImage
      description
      accountId
      websiteLink
      twitterLink
    }
  }
}
