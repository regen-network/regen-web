// TODO
// export function truncate(str: string, length: number) {
//   const sentences = str.match(/[^\.!\?]+[\.!\?]+/g);
//   if (sentences) {
//     // for (let i )
//     console.log(sentences);
//     if (sentences.length > length) {
//       return sentences.slice(0, length).join(' ');
//     }
//   }
//   return str;
// }
