query partyById($id: UUID!) {
  partyById(id: $id) {
    id
    name
    type
    image
    bgImage
    description
    accountId
    websiteLink
    twitterLink
    walletByWalletId {
      addr
    }
  }
}
