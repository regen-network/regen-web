import { PaymentOptionsType } from './PaymentInfoForm.types';

type PaymentInfoFormProps = {
  paymentOption: PaymentOptionsType;
};
export const PaymentInfoForm = ({ paymentOption }: PaymentInfoFormProps) => {};
