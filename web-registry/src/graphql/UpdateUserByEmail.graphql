mutation UpdateUserByEmail($input: UpdateUserByEmailInput!) {
  updateUserByEmail(input: $input) {
    user {
      partyId
      phoneNumber
      roleTitle
    }
    partyByPartyId {
      name
      description
      image
    }
  }
}
