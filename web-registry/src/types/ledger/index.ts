import { BatchInfo } from '@regen-network/api/lib/generated/regen/ecocredit/v1alpha1/types';

export type BasketEcocredit = {
  batchInfo: BatchInfo;
  projectName: string;
  projectDisplay: string;
};
