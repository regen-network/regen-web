import { makeStyles } from '@mui/styles';

export const useUserMenuItemsStyles = makeStyles(theme => ({
  paper: {
    padding: `${theme.spacing(2.5)} ${theme.spacing(2.5)} ${theme.spacing(
      3.75,
    )} ${theme.spacing(2.5)}`,
  },
}));
