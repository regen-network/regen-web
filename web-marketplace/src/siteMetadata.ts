import { msg } from '@lingui/macro';

const siteMetadata = {
  title: msg`Regen Marketplace`,
  description: msg`Buy and sell ecosystem service credits at the open marketplace for climate solutions.registry`,
  author: msg`Regen Network`,
  siteUrl: 'https://app.regen.network',
};

export default siteMetadata;
