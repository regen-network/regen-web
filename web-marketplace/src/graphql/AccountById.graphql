query AccountById($id: UUID!) {
  accountById(id: $id) {
    id
    name
    type
    image
    bgImage
    description
    websiteLink
    twitterLink
    addr
    nonce
    hideEcocredits
    hideRetirements
    custodialAddress
  }
}
