name: CI

on:
  push:
    branches: [ master ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: install
      run: |
        yarn install
        yarn bootstrap

    - name: build-app
      run: yarn build

    - name: test
      run: yarn test
