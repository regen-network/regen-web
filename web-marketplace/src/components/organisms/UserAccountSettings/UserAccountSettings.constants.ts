import { msg } from '@lingui/macro';

export const EMAIL_ADDED = msg`Log in email successfully added`;
export const COPY_ADDRESS = msg`Copy address`;
export const ADDRESS_COPIED = msg`Address copied`;
