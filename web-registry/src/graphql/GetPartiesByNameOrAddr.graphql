query GetPartiesByNameOrAddr($input: String) {
  getPartiesByNameOrAddr(input: $input) {
    nodes {
      id
      accountId
      name
      type
      image
      description
      walletByWalletId {
        addr
      }
    }
  }
}