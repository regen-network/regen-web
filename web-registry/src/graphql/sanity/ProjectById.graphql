query ProjectById($id: String!) {
  allProject(where: { projectId: { eq: $id } }) {
    credibilityCards {
      credibilityCard {
        title
        descriptionRaw
        icon {
          asset {
            altText
            url
          }
        }
      }
      claims {
        description
      }
    }
  }
}
