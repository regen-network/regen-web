query AllCreditClasses {
  allCreditClasses {
    nodes {
      uri
      creditClassVersionsById(orderBy: CREATED_AT_DESC, first: 1) {
        nodes {
          name
          description
          image
        }
      }
    }
  }
}
