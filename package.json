{
  "name": "root",
  "private": true,
  "dependencies": {
    "lerna": "^3.0.0-rc.0"
  },
  "scripts":{
    "postinstall": "lerna bootstrap && lerna run build --stream",
    "start":"lerna run --scope=regen-network-mvp-server start --stream",
    "test":"echo 'No tests yet'"
  }
}
