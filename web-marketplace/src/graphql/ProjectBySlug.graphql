fragment accountFields on Account {
  id
  creatorId
  type
  name
  description
  image
  websiteLink
  twitterLink
  addr
}

query ProjectBySlug($slug: String!) {
  projectBySlug(slug: $slug) {
    ...projectFields
  }
}
