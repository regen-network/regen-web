import Section from 'web-components/src/components/section';

import { DATA_STREAM } from './ProjectDetails.constant';

export const DataStream = () => {
  return <Section title={DATA_STREAM}></Section>;
};
