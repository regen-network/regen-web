query AccountByAddr($addr: String!) {
  accountByAddr(addr: $addr) {
    id
    addr
    name
    type
    image
    bgImage
    description
    websiteLink
    twitterLink
    hideEcocredits
    hideRetirements
  }
}
