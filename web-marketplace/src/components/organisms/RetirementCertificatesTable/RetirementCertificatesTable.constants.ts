import { msg } from '@lingui/macro';

export const ECOCREDIT_BATCH_ID_TOOLTIP = msg`Each credit batch has a unique ID (i.e. denomination) that starts with the credit class ID followed by the project ID, start date and end date of the vintage year, and ending in the batch sequence number.`;
