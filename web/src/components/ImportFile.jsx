import React, { Component } from 'react';


class ImportFile extends React.Component {
    render() {
        return (
            console.log("not a fan of console.log -style debugging.")
        );
    }
}

export default (ImportFile);
