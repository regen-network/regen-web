import { SxProps } from '@mui/material';

export const loaderStyles = {
  withLoaderBlock: {
    display: 'flex',
    width: '100%',
    justifyContent: 'center',
    flexDirection: 'col',
    alignItems: 'center',
    py: 25,
  } as SxProps,
};
