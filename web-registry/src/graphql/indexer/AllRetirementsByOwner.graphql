query IndexerAllRetirementsByOwner($owner: String!) {
  allRetirements(condition: { owner: $owner }) {
    nodes {
      nodeId
      owner
      amount
      reason
      batchDenom
      jurisdiction
      timestamp
      reason
    }
  }
}
