query walletByAddr($addr: String!) {
  walletByAddr(addr: $addr) {
    id
    addr
    projectsByAdminWalletId {
      nodes {
        id
        onChainId
        handle
        metadata
      }
    }
  }
}
