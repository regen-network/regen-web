query GetUserProfileByEmail($email: String!) {
  userByEmail(email: $email) {
    email
    id
    isAdmin
    phoneNumber
    partyId
    roleTitle
    partyByPartyId {
      name
      walletId
      description
      image
    }
  }
}
